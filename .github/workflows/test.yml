name: Test

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Setup repo
        uses: actions/checkout@v4

      - name: Install HeyCart CLI
        uses: ./.

      - name: Test binary
        run: heycart-cli --version

      - name: Clone HeyPlatformDemoData
        run: git clone https://github.com/heycart/HeyPlatformDemoData.git

      - name: Zip HeyPlatformDemoData
        run: heycart-cli extension zip HeyPlatformDemoData

      - name: Validate
        run: heycart-cli extension validate HeyPlatformDemoData-*.zip
